import React from "react";
import { AMachine, Sensor } from "../../../models/Store";


const MachineItem: React.FC<{ machine: AMachine }> = (props) => {

    return (
        <>
            <div>Test</div>
        </>
    );
};

export default MachineItem;
